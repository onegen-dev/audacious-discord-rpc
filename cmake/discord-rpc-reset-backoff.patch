diff --git a/src/discord-rpc.cpp b/src/discord-rpc.cpp
index 8e9b086..73e3955 100644
--- a/src/discord-rpc.cpp
+++ b/src/discord-rpc.cpp
@@ -95,6 +95,10 @@ namespace discord {
             return *this;
         }
 
+        // A manual reconnect must not inherit the previous connection's backoff.
+        m_nextConnect = std::chrono::system_clock::now();
+        Backoff::get().reset();
+
         m_ioWorker = new(std::nothrow) IOWorker();
         if (m_ioWorker) {
             m_ioWorker->start();
@@ -111,16 +115,16 @@ namespace discord {
             return *this;
         }
 
+        // Prevent the final update from reopening the old IPC connection.
+        m_initialized = false;
+
         if (m_ioWorker) {
             delete m_ioWorker;
             m_ioWorker = nullptr;
         }
 
         this->clearPresence();
-        this->update();
-
         Connection::destroyInstance();
-        m_initialized = false;
 
         return *this;
     }
